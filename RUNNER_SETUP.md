# Self-Hosted Runner Setup — pointer

The full, executable runbook is at
**[`docs/runner-setup.md`](docs/runner-setup.md)**. This file is a
single-line pointer for discoverability from the repo root.

If you have just cloned the repo and want to enable the
`.github/workflows/benchdeck-product-test.yml` workflow on a self-hosted
runner (Windows 11 + WSL2 Ubuntu recommended), open
`docs/runner-setup.md` and follow the 9-phase checklist end to end.
