---
description: Run the approved real-OpenAI BenchDeck integration suite with strict budgets
agent: benchdeck-product-tester
---
Run the minimal complete real-provider validation for BenchDeck using `benchdeck_live_run`. Require a dedicated key file, explicit approval, exact models, strict request/token budgets, domain-allowlisted egress, and an ephemeral container. Never reveal the key and never substitute a fake. Requested model or scope: $ARGUMENTS
