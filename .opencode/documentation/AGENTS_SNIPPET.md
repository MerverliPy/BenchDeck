# Optional AGENTS.md Integration Snippet

Merge the relevant lines below into the repository's existing `AGENTS.md`. Do not replace established repository instructions.

```markdown
## Documentation maintenance

- Use `@repository-docs` or `/docs-update` for evidence-driven documentation maintenance.
- Documentation claims must reflect executable behavior, tests, public interfaces, active configuration, and enabled implementation.
- Roadmaps, issues, proposals, TODOs, stubs, and disabled code must not be described as currently available features.
- The documentation agent may apply low-risk documentation corrections directly, but deletions, moves, README restructuring, compatibility/support changes, policy changes, release-history changes, major documentation hierarchy changes, and commits require explicit approval.
- The documentation agent must not modify implementation source code; implementation conflicts require a coding-agent handoff.
- Validation results must be reported as Passed, Failed, or Not run.
```
