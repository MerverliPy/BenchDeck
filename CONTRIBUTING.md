# Contributing

1. Create a focused branch.
2. Install with `pip install -e '.[dev]'`.
3. Run `ruff check .` and `pytest`.
4. Keep artifact formats backward-compatible or document the migration.
5. Add a regression test for every benchmark-runner defect fixed.
